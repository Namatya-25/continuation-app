# MY DISASTER

勉強を続けるために、災害を育てよう。

HTML + CSS + 素のJavaScript（ESモジュール）で作られています。フレームワークもビルドも使っていません。

---

## 動かす

ESモジュールを使っているため、`index.html` をダブルクリックで開くと動きません（ブラウザがCORSでブロックします）。ローカルサーバーを立ててください。

```bash
# どれか1つ
npx serve .
python3 -m http.server 8000
npx vite            # 保存すると即反映されるのでおすすめ
```

Vite を使う場合は `npm init -y && npm i -D vite` してから `npx vite` で立ち上がります。設定ファイルは要りません。

## 公開する

ビルド不要なので、このフォルダをそのまま置くだけです。

- GitHub Pages：リポジトリの Settings → Pages でブランチを指定
- Netlify / Vercel：フォルダをドラッグ&ドロップ

---

## ファイル構成

```
index.html          画面のマークアップ
styles.css          スタイル
src/
  data/
    levels.js       レベル閾値・階級・破壊対象・街の配置    ← バランス調整はここ
    copy.js         「今日の1歩」の文言・キャラのセリフ     ← 文言調整はここ
  lib/
    state.js        アプリ状態の定義
    storage.js      localStorage への読み書き（唯一の窓口）
    date.js         04:00区切りの日付判定
    domain.js       レベル計算・減衰・達成処理             ← 全員が使う。触る前に相談
  ui/
    scope.js        レーダースコープの描画
    city.js         被害区域の描画
    records.js      カレンダー・統計の描画
  main.js           画面更新とイベント登録
```

### 触るファイルの分け方（3人想定）

| 担当 | ファイル |
| --- | --- |
| A | `ui/scope.js` — 災害の見た目、成長演出 |
| B | `ui/city.js` — 街、破壊演出（ここが「推し技術」） |
| C | `ui/records.js` + `data/copy.js` — 記録画面、文言 |

`lib/domain.js` と `main.js` は共有部分です。最初に3人で仕様を確認してから固定し、以後はなるべく触らないようにするとコンフリクトが起きません。

---

## 全体のルール

**状態を変えたら `persist()` → `render()` を呼ぶ。** これだけです。

```js
S.streak.currentDays += 1;   // 状態を書き換える
persist();                   // localStorage に保存
render();                    // 画面を描き直す
```

`S` は `lib/state.js` が公開しているアプリ全体の状態です。ESモジュールのライブバインディングなので、どのファイルから import しても同じ実体を見ています。

`localStorage` を直接触っていいのは `lib/storage.js` だけです。将来サーバーに移すとき、そこだけ差し替えれば済むようにしてあります。

---

## 動作確認

設定タブの「開発用」から操作できます。

| ボタン | 動き |
| --- | --- |
| 1日進める | 翌日にする。連続日数の判定を試せる |
| 3日サボる | 3日進める。弱体化の挙動を試せる |
| 30日分を記録する | Lv.30 巨大竜巻の状態を作る |
| すべて消す | 初期状態に戻す |

公開前にこのセクション（`index.html` の「開発用」ブロックと `main.js` の該当ハンドラ）を削除してください。

---

## 仕様の要点

- **1日の区切りは 04:00。** 深夜の勉強を前日扱いにするため
- **サボってもリセットしない。** 1日は猶予、2〜3日で弱体化、4日目から1日ずつ減算。下限は1階級下まで
- **レベル閾値**は Lv1 = 1日目、Lv2 = 3日目、以降1日ごと（モックの「次のレベルまであと2日」に合わせています）
- **データは端末内のみ。** サーバーに送信していません。バックアップは設定タブのJSON書き出しから

詳細は `MY_DISASTER_仕様書.md` を参照してください。

## まだ作っていないもの

- サボり券の使用処理（溜まるだけで使えない）
- 破壊演出の作り込み（現状はシェイクと瓦礫への差し替えのみ）
- 複数の災害種（地震・洪水など）
